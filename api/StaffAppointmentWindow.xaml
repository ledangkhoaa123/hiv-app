<Window x:Class="api.StaffAppointmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:api"
        mc:Ignorable="d"
        Title="StaffAppointmentWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nút Tải lại -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Tải lại" Width="100" Click="ReloadButton_Click"/>
        </StackPanel>

        <!-- Bảng dữ liệu -->
        <DataGrid x:Name="AppointmentDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Bệnh nhân" Binding="{Binding PatientName}" Width="*" />
                <DataGridTextColumn Header="Bác sĩ" Binding="{Binding DoctorName}" Width="*" />
                <DataGridTextColumn Header="Dịch vụ" Binding="{Binding ServiceName}" Width="*" />
                <DataGridTextColumn Header="Thời gian" Binding="{Binding Time}" Width="*" />
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*" />
                <DataGridTemplateColumn Header="Hành động" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Duyệt" Margin="2" Padding="4,2" Click="ApproveButton_Click" Tag="{Binding AppointmentId}" />
                                <Button Content="Từ chối" Margin="2" Padding="4,2" Click="RejectButton_Click" Tag="{Binding AppointmentId}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
